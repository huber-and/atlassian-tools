<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>io.github.huber-and.atlassian</groupId>
	<artifactId>atlassian-tools</artifactId>
	<version>${revision}${changelist}</version>
	<packaging>pom</packaging>
	<name>Atlassian Tools</name>
	<description>A comprehensive suite of Java tools and libraries for
		integrating with Atlassian Confluence, including a REST API client, HTML
		publishing utility, and Maven plugin.</description>
	<url>https://github.com/huber-and/atlassian-tools</url>
	<properties>
		<revision>0.2.3</revision>
		<changelist>-SNAPSHOT</changelist>
		<maven.compiler.source>21</maven.compiler.source>
		<maven.compiler.target>21</maven.compiler.target>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<enforcer.plugin.version>3.6.3</enforcer.plugin.version>
		<flatten.plugin.version>1.8.0</flatten.plugin.version>
		<gpg.plugin.version>3.2.8</gpg.plugin.version>
		<javadoc.plugin.version>3.12.0</javadoc.plugin.version>
		<maven-plugin.version>3.9.0</maven-plugin.version>
		<source.plugin.version>3.4.0</source.plugin.version>
		<surefire.plugin.version>3.5.6</surefire.plugin.version>
		<compiler.plugin.version>3.15.0</compiler.plugin.version>
		<jacoco.plugin.version>0.8.15</jacoco.plugin.version>
		<maven-plugin-tools.version>3.15.2</maven-plugin-tools.version>
		<central-publishing.plugin.version>0.11.0</central-publishing.plugin.version>
		<openapi-generator.plugin.version>7.24.0</openapi-generator.plugin.version>
		<frontend.plugin.version>2.0.2</frontend.plugin.version>
		<shade.plugin.version>3.6.1</shade.plugin.version>
		<jar.plugin.version>3.5.0</jar.plugin.version>
		<!-- Minimum Maven version: central-publishing-maven-plugin requires >= 3.9.2 -->
		<minimum.maven.version>3.9.2</minimum.maven.version>

		<!-- Shared dependency versions, applied via dependencyManagement below -->
		<slf4j.version>2.0.18</slf4j.version>
		<lombok.version>1.18.46</lombok.version>
		<jsoup.version>1.23.1</jsoup.version>
		<commons-lang3.version>3.20.0</commons-lang3.version>
		<jackson.version>2.22.1</jackson.version>
		<junit.version>6.1.3</junit.version>
		<jakarta-annotation.version>3.0.0</jakarta-annotation.version>
		<swagger-annotations.version>2.2.53</swagger-annotations.version>
		<httpclient5.version>5.6.3</httpclient5.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<!-- BOM imports: keep the whole family on one consistent version -->
			<dependency>
				<groupId>org.junit</groupId>
				<artifactId>junit-bom</artifactId>
				<version>${junit.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>${jackson.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-simple</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.projectlombok</groupId>
				<artifactId>lombok</artifactId>
				<version>${lombok.version}</version>
			</dependency>
			<dependency>
				<groupId>org.jsoup</groupId>
				<artifactId>jsoup</artifactId>
				<version>${jsoup.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>${commons-lang3.version}</version>
			</dependency>
			<dependency>
				<groupId>jakarta.annotation</groupId>
				<artifactId>jakarta.annotation-api</artifactId>
				<version>${jakarta-annotation.version}</version>
			</dependency>
			<dependency>
				<groupId>io.swagger.core.v3</groupId>
				<artifactId>swagger-annotations</artifactId>
				<version>${swagger-annotations.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.httpcomponents.client5</groupId>
				<artifactId>httpclient5</artifactId>
				<version>${httpclient5.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<licenses>
		<license>
			<name>The Apache License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>
	<developers>
		<developer>
			<name>Andreas Huber</name>
			<email>andreas@the-hubers.de</email>
			<url>https://github.com/huber-and/atlassian-tools</url>
		</developer>
	</developers>
	<scm>
		<connection>scm:git:git://github.com/huber-and/atlassian-tools.git</connection>
		<developerConnection>
			scm:git:ssh://github.com:huber-and/atlassian-tools.git</developerConnection>
		<url>https://github.com/huber-and/atlassian-tools/tree/main</url>
	</scm>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>${source.plugin.version}</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>${javadoc.plugin.version}</version>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<failOnError>false</failOnError>
				</configuration>

			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-gpg-plugin</artifactId>
				<version>${gpg.plugin.version}</version>
				<executions>
					<execution>
						<id>sign-artifacts</id>
						<phase>verify</phase>
						<goals>
							<goal>sign</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${compiler.plugin.version}</version>
				<configuration>
					<!-- Verified clean across all three Java modules, including the OpenAPI-generated
					     client, before enabling: zero warnings under -Xlint:all as of this change. -->
					<compilerArgs>
						<arg>-Xlint:all,-options,-processing</arg>
					</compilerArgs>
					<failOnWarning>true</failOnWarning>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.plugin.version}</version>
				<configuration>
					<!-- wiki-client is generated from Atlassian's OpenAPI specification and has
					     no hand-written logic to cover; measuring it would only dilute the
					     number for the code that is actually maintained here. -->
					<excludes>
						<exclude>net/atlassian/wiki/rest/**</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>flatten-maven-plugin</artifactId>
				<version>${flatten.plugin.version}</version>
				<inherited>true</inherited>
				<configuration>
					<updatePomFile>true</updatePomFile>
					<flattenMode>resolveCiFriendliesOnly</flattenMode>
				</configuration>
				<executions>
					<execution>
						<id>flatten</id>
						<phase>process-resources</phase>
						<goals>
							<goal>flatten</goal>
						</goals>
					</execution>
					<execution>
						<id>flatten.clean</id>
						<phase>clean</phase>
						<goals>
							<goal>clean</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>${enforcer.plugin.version}</version>
				<inherited>true</inherited>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>${minimum.maven.version}</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>${central-publishing.plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<publishingServerId>central</publishingServerId>
					<autoPublish>true</autoPublish>
					<excludeArtifacts>
						<param>architecture-docs</param>
					</excludeArtifacts>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<plugins>
				<!-- Pinned explicitly: the Maven 3.9 default surefire predates
				JUnit Platform 6 and would silently skip all tests -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>${surefire.plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-plugin-plugin</artifactId>
					<version>${maven-plugin-tools.version}</version>
					<executions>
						<execution>
							<id>help-mojo</id>
							<goals>
								<!-- good practice is to generate help mojo for
								plugin -->
								<goal>helpmojo</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
	<modules>
		<module>wiki-client</module>
		<module>wiki-publisher</module>
		<module>maven-plugin</module>
		<module>cli</module>
		<module>architecture-docs</module>
	</modules>

	<profiles>
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-enforcer-plugin</artifactId>
						<version>${enforcer.plugin.version}</version>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>${minimum.maven.version}</version>
								</requireMavenVersion>
								<reactorModuleConvergence>
									<message>The reactor is not valid</message>
								</reactorModuleConvergence>
								<requireReleaseDeps>
									<message>No Snapshots Allowed!</message>
								</requireReleaseDeps>
								<requireReleaseVersion>
									<message>No Snapshots Allowed!</message>
								</requireReleaseVersion>
							</rules>
							<fail>true</fail>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

	</profiles>
</project>

